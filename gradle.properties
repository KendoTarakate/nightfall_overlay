# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx12G
org.gradle.parallel=true

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=1.20.1
yarn_mappings=1.20.1+build.10
loader_version=0.16.14
# Architectury Loom replaces Fabric Loom so a single build can produce both
# the Fabric and the Forge artifacts (it is a drop-in superset of Fabric Loom).
loom_version=1.11.454

# Forge Properties
forge_version=1.20.1-47.4.20

# Mod Properties
mod_version=1.1.0
maven_group=net.nightfall
archives_base_name=nightfalloverlay

# Dependencies
fabric_version=0.92.6+1.20.1